# ============================================================================
# JAGUAR AI — production requirements
# ============================================================================
# Core GUI (Flask)
Flask
flask-login
python-dotenv
Werkzeug
itsdangerous

# Voice I/O
SpeechRecognition
pyttsx3
PyAudio            # optional - the listener falls back if it can't be imported
pygame             # replaces playsound so the Stop button really interrupts TTS
gTTS

# System automation
pyautogui
pywhatkit
pyperclip
psutil

# File / image reading
PyPDF2
python-docx
pytesseract
Pillow

# Firebase persistence
firebase-admin

# Streamlit alternative UI
streamlit
streamlit-autorefresh
PyQt6

# Multi-LLM
openai
anthropic
google-generativeai
ollama

# ============================================================
# NEW: Autonomous Agent + Mobile-Ready Backend (JAGUAR AI v2)
# ============================================================

# FastAPI mobile-ready backend (run with: uvicorn api.mobile_api:app)
fastapi
a2wsgi
uvicorn[standard]
pydantic
python-multipart     # for /voice file uploads
httpx                 # used by some test scripts

# Vector memory (semantic recall)
sentence-transformers   # embedder; falls back to hashed BoW if missing
numpy                   # required by faiss

# Pick ONE of these for the vector index. Both have graceful fallback
# to an in-memory cosine index if neither is installed.
faiss-cpu               # local, fast, file-based
# chromadb               # persistent client/server - uncomment if you prefer

# Browser automation (Playwright). Install browsers separately with:
#     playwright install chromium
playwright

# Voice: STT (Whisper) and high-quality TTS
openai-whisper          # heavy; offline-capable once installed
edge-tts                # lightweight TTS using Microsoft Edge voices
